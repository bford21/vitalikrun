# Database (Railway will auto-inject DATABASE_URL in production)
DATABASE_URL=postgresql://user:password@localhost:5432/vitalikrun

# Alchemy API Key (get from https://alchemy.com)
ALCHEMY_API_KEY=your_alchemy_api_key_here

# Server Port (Railway auto-assigns PORT in production)
PORT=8080

# Frontend URL (use * for production CORS, or specific URL for dev)
FRONTEND_URL=*

# Node Environment
NODE_ENV=production
